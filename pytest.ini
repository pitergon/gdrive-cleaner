[pytest]
testpaths = tests
addopts = -ra --capture=fd
# addopts = -ra -s
#addopts = -ra -s -o log_cli=true -o log_cli_level=INFO
markers =
    integration: real Google Drive integration tests (requires explicit env setup)

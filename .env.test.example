# Enable integration tests explicitly
RUN_GDRIVE_INTEGRATION=0

# Absolute path to service account key JSON (local machine path)
GOOGLE_APPLICATION_CREDENTIALS=D:/path/to/service_account.json

# Parent folder ID dedicated for test artifacts
TEST_DRIVE_FOLDER_ID=replace_with_test_folder_id
